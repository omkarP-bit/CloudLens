import { Placeholder } from '../Placeholder.js';

export function Terminal() {
  return (
    <Placeholder
      title="Cloud Terminal Shell"
      description="Access-controlled browser terminal executing authenticated AWS CLI command lines in a sandboxed session."
    />
  );
}
